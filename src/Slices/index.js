import allEvents, {
  fetchStart,
  fetchSuccess,
  fetchFailure,
} from "./events.slice";

import allFeeds, {
  fetchFeeds,
  fetchFeedsSuccess,
  fetchFeedsFailure,
} from "./feeds.slice";

export {
  allEvents,
  fetchStart,
  fetchSuccess,
  fetchFailure,
  allFeeds,
  fetchFeeds,
  fetchFeedsSuccess,
  fetchFeedsFailure,
};
